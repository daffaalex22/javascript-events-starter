// KeyDown Event Starter
// Add your keydown event listeners here

// Get references to the input and display elements
const textInput = document.getElementById('textInput');
const info = document.getElementById('info');
const keyDisplay = document.getElementById('keyDisplay');

// TODO: Add keydown event listener to the document
// When any key is pressed, update the keyDisplay with the key that was pressed

// TODO: Add keydown event listener to the text input
// When a key is pressed in the input, update the info with the key that was pressed

// TODO: Add a keydown event listener that detects special keys
// Check for Enter, Escape, Space, and Arrow keys

// TODO: Add a keydown event listener that detects modifier keys
// Check if Shift, Ctrl, or Alt is pressed along with another key

// TODO: Add a keydown event listener that prevents default behavior
// Prevent the default action for the Space key in the input